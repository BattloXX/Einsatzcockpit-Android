<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SMS-Versand über SIM-Karte -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Hintergrund-Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Neustart nach Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Akku-Optimierung ausschalten (für Dauerbetrieb) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Netzwerkstatus für ConnectivityManager-Callback (sofortiger Reconnect bei Netzwechsel) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WakeLock: CPU wach halten damit WebSocket-Pings auch im Hintergrund feuern -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Benachrichtigungen für Foreground-Service (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>

        <service
            android:name="cloud.einsatzleiter.smsgatewayplugin.SmsGatewayService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            android:stopWithTask="false" />

        <service
            android:name="cloud.einsatzleiter.smsgatewayplugin.DeviceKeepaliveService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            android:stopWithTask="false" />

        <receiver
            android:name="cloud.einsatzleiter.smsgatewayplugin.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
