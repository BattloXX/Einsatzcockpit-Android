<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SMS-Versand über SIM-Karte -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- SMS-Empfang (optional, Org-Schalter + Laufzeit-Berechtigung).
         Wird nur bei aktivem Empfang fuer den dynamisch registrierten
         Receiver in SmsGatewayService benoetigt (siehe updateSmsReceiver()). -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Hintergrund-Service.
         specialUse statt dataSync: dataSync hat unter Android 15 (targetSdk 35) ein
         kumulatives Laufzeitlimit von ~6 h/24 h, danach beendet das System den Service
         (onTimeout) -> Verbindung bricht "nach einigen Stunden" ab. specialUse hat kein
         Zeitlimit und ist fuer dauerhafte, nicht anderweitig kategorisierte Aufgaben
         (persistente Gateway-Verbindung) vorgesehen. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Neustart nach Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Akku-Optimierung ausschalten (für Dauerbetrieb) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Netzwerkstatus für ConnectivityManager-Callback (sofortiger Reconnect bei Netzwechsel) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WakeLock: CPU wach halten damit WebSocket-Pings auch im Hintergrund feuern -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Benachrichtigungen für Foreground-Service (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>

        <service
            android:name="cloud.einsatzleiter.smsgatewayplugin.SmsGatewayService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Persistente WebSocket-Verbindung zum Versand von Einsatz-SMS ueber die SIM-Karte (SMS-Gateway-Betrieb rund um die Uhr)." />
        </service>

        <service
            android:name="cloud.einsatzleiter.smsgatewayplugin.DeviceKeepaliveService"
            android:foregroundServiceType="specialUse"
            android:exported="false"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Haelt die Einsatz-Geraete-Anzeige im Dauerbetrieb aktiv, damit Live-Updates auch bei ausgeschaltetem Bildschirm empfangen werden." />
        </service>

        <receiver
            android:name="cloud.einsatzleiter.smsgatewayplugin.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
